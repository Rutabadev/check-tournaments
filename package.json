{
  "name": "check-tournaments",
  "version": "1.0.0",
  "description": "",
  "main": "main.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.mjs --platform=node --target=es2020 --format=esm --bundle --external:@aws-sdk --external:nodemailer --external:uuid --outfile=dist/index.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.445.0",
    "@aws-sdk/client-s3": "^3.511.0",
    "cheerio": "^1.0.0-rc.12",
    "esbuild": "^0.20.0",
    "nodemailer": "^6.9.7"
  }
}
