{
  "name": "check-tournaments",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "start": "RUN_MODE=local node run-local.mjs",
    "build": "node esbuild.config.mjs",
    "test": "vitest",
    "test:run": "vitest run",
    "docker:build": "npm run build && docker build -t check-tournaments .",
    "docker:start": "docker run --rm -p 9000:8080 --name ct-local check-tournaments",
    "docker:invoke": "curl -s -XPOST \"http://localhost:9000/2015-03-31/functions/function/invocations\" -d \"{}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.445.0",
    "@sparticuz/chromium": "^132.0.0",
    "dotenv": "^17.2.3",
    "nodemailer": "^6.9.7",
    "puppeteer-core": "^24.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "prettier": "^3.8.1",
    "puppeteer": "^24.1.1",
    "vitest": "^4.0.16"
  }
}
